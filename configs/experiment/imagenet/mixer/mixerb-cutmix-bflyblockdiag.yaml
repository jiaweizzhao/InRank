# @package _global_

# to execute this experiment run:
# python run.py experiment=example_simple.yaml

defaults:
  - /experiment/imagenet/mixer/mixerb-cutmix.yaml
  - override /model/channel_mlp_cfg: bflyblockdiag

model:
  drop_path_rate: 0.0
