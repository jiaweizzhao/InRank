# @package _global_
defaults:
  - /experiment/lra/textc/transformer
  - override /model/attn_cfg: sblocal

model:
  attn_cfg:
    attention_dropout: 0.1
    local_context: 32
    nb_features: 16
    softmax_eps: 1e-4
