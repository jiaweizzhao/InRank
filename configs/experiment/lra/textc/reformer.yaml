# @package _global_
defaults:
  - /experiment/lra/textc/transformer
  - override /model/attn_cfg: reformer

model:
  mha_cfg:
    shared_qk: True
  attn_cfg:
    attention_dropout: 0.1
    bucket_size: 32
    n_hashes: 2
